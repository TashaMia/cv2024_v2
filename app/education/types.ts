export type educationBdType = {
  title: string;
  company: string;
  time: string;
};

export type certificateType = {
  title: string;
  time: string;
  link: string;
};

export type certificateImagesType = {
  title: string;
  url: string;
};
